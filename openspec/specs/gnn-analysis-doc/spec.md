### 需求: 设计动机与问题分析

`docs/gnn-attack-design.md` 必须阐明 GNN 增强攻击方法的设计动机，说明原始方法的局限性以及 GNN 方法的核心改进点。

#### 场景: 原始方法局限性分析

- **当** 文档读者阅读"设计动机"章节
- **那么** 文档说明原始方法仅利用大小为 2 的直接交集推断邻接关系，信息利用效率受限

#### 场景: GNN 方法优势说明

- **当** 文档读者阅读"核心改进"章节
- **那么** 文档说明 GNN 从所有响应大小的共现模式中学习，使用软阈值替代硬性交集条件

### 需求: 架构演进说明

`docs/gnn-attack-design.md` 必须记录从 v0（矩阵输入）到 v1（固定特征输入）的架构演进过程，解释核心设计决策。

#### 场景: 架构演进对比

- **当** 文档读者阅读"架构演进"章节
- **那么** 文档包含 v0 和 v1 的架构对比 ASCII 图，清晰展示 input_dim 耦合问题的根源
- **而且** 解释固定维度节点特征的设计理由（16 维统计量的选取依据）

### 需求: 数据流与组件关系

`docs/gnn-attack-design.md` 必须包含完整的数据流图和组件依赖关系说明。

#### 场景: 训练数据流

- **当** 文档读者阅读"训练阶段"章节
- **那么** 文档展示从场景配置 → 点集生成 → 响应枚举 → 共现矩阵 → 特征提取 → GNN 训练 → 模型保存的完整数据流 ASCII 图

#### 场景: 推理数据流

- **当** 文档读者阅读"推理阶段"章节
- **那么** 文档展示从响应加载 → 共现矩阵 → 特征提取 → 消息传递 → 边预测 → 图构建 → 评估的完整数据流 ASCII 图

### 需求: 关键算法描述

`docs/gnn-attack-design.md` 必须包含关键算法的文字描述或伪代码。

#### 场景: 算法说明

- **当** 文档读者需要理解核心算法
- **那么** 文档包含 `extract_node_features()`、`extract_edge_features()`、GraphSAGE 消息传递、Focal Loss 训练的算法步骤说明
- **而且** 说明各算法的输入输出类型和维度

### 需求: 与原始方法对比

`docs/gnn-attack-design.md` 必须包含 GNN 方法与原始集合交集方法的系统对比。

#### 场景: 对比表

- **当** 文档读者需要选择攻击方法
- **那么** 文档包含表格对比 GNN 和 Original 在以下维度的差异：信息利用方式、对采样率的鲁棒性、跨数据集泛化能力、计算复杂度、已知限制
