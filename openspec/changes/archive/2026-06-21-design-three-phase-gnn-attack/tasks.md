## 1. 概要设计文档 `docs/design-three-phase-summary.md`

- [x] 1.1 撰写攻击场景全景——攻击者是谁、能拿到什么数据（共现矩阵）、不能拿到什么（坐标/标签）、最终想要什么（2D 分布形状）
- [x] 1.2 绘制三阶段完整数据流图——标注每个阶段的输入来源、输出去向、是否需要标签
- [x] 1.3 撰写与原始方法（集合交集）和当前 GNN 方案的对比表——信息利用方式、数据依赖、输出类型、假设前提

## 2. 详细设计文档 `docs/design-three-phase-detailed.md` — Phase 1 预训练

- [x] 2.1 定义 Phase 1 输入输出——数据来源（攻击者自生成）、数据格式要求（形状类型、N范围、采样率范围、每种场景占比）、输出格式（GNN checkpoint 必须包含的字段）
- [x] 2.2 给出训练数据生成器的算法伪代码——形状生成 → 模拟加密查询 → 共现矩阵 → 标签（曼哈顿相邻）
- [x] 2.3 说明共现图消息传递设计——与 k-NN 方案的区别、注意力聚合的数学原理、为什么共现加权图更合理
- [x] 2.4 定义 Phase 1 验证标准——AUC>0.7 的计算方式、验证集构成要求（必须含 N≥200 场景）、多阈值 P/R 输出格式

## 3. 详细设计文档 `docs/design-three-phase-detailed.md` — Phase 2 自训练

- [x] 3.1 定义 Phase 2 输入输出——数据来源（目标加密查询响应 → 共现矩阵 C[N,N]）、输入格式（仅 C[N,N] 矩阵 + Phase 1 模型，不依赖源代码或明文坐标）、输出格式（推断边集合 E_hat = {(i,j,prob)}）
- [x] 3.2 给出伪标签筛选算法伪代码——Top-K 选取逻辑（K≈2N）、Bottom-L 选取逻辑（L≈10N）、不确定区域丢弃、区域多样性约束
- [x] 3.3 给出迭代微调算法伪代码——学习率（≤预训练 lr/10）、冻结策略（仅微调预测器和最后层消息传递）、一致性正则化公式（C 扰动 → C'、要求 GNN(C)≈GNN(C')）
- [x] 3.4 定义收敛判定标准——边集合重合率≥90% 的计算方式、最大迭代轮数、发散检测（连续N轮重合率持续下降）

## 4. 详细设计文档 `docs/design-three-phase-detailed.md` — Phase 3 形状重建

- [x] 4.1 定义 Phase 3 输入输出——数据来源（Phase 2 输出的 E_hat）、输入格式（(i,j,prob) 三元组列表）、输出格式（pos[N,2] 的 2D 坐标数组）
- [x] 4.2 给出力导向布局算法伪代码——弹簧-斥力能量函数公式、迭代收敛条件（所有节点位移<ε）、初始位置初始化策略
- [x] 4.3 给出可选的 Procrustes 对齐和 Hausdorff 评估伪代码
- [x] 4.4 定义重建失败检测——边数 < N/2 判定、布局方差过小判定、连通分量数 > N/4 判定

## 5. 旧设计文档清理

- [x] 5.1 确认 `docs/gnn-attack-design.md` 内容已被三阶段设计覆盖 → 删除
- [x] 5.2 确认 `docs/analysis-current-issues.md` 问题分析已整合 → 删除
- [x] 5.3 确认 `docs/design-incremental-improvement.md` 改进思路已被吸收 → 删除
- [x] 5.4 确认 `docs/design-full-improvement.md` 全量改进已被吸收 → 删除

## 6. 设计文档关联性检查

- [x] 6.1 确认概要设计和详细设计中对三阶段输入输出的描述一致
- [x] 6.2 确认 Phase 1→Phase 2 的数据格式约定在详细设计中显式定义
- [x] 6.3 确认 Phase 2→Phase 3 的数据格式约定在详细设计中显式定义
- [x] 6.4 在详细设计末尾加入「代码实现关联要求」章节——明确后续 gnn-attack 代码实现必须对齐的接口点列表
