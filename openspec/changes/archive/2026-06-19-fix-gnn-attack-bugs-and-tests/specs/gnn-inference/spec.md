## 修改需求

### 需求: 节点特征提取

`extract_node_features()` 必须确保 16 个特征维度都提供互不冗余的信息。F3/F5/F7 禁止使用相同的计算公式。

#### 场景: 特征维度互不冗余

- **当** 输入 N×N 共现矩阵（N≥4）
- **那么** F3 为共现稀疏度 `nonzero/N`、F5 为共现偏度 `skew(vec)`、F7 为共现峰度 `kurtosis(vec)`
- **而且** F3 ≠ F5 ≠ F7（当 N>4 且共现向量有差异时）
